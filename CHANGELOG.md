# 版本记录

版本号以 [VERSION](VERSION) 为准。发布使用 `vX.Y.Z` Git 标签，更新渠道为本仓库 `main`；只有版本号升高才提示更新。

## 1.2.0 - 2026-09-16

- 源更新差异审核仅适用于源 PSD 更新，其他入口按各自授权执行；“历史记录”统一指当前同两份说明内的内容，不另建当前说明中的历史记录。
- 异常只报告实际状态及已写文件，文件还原由用户处理；正常重生成后的适配恢复继续执行。
- 新选中框等视觉节点接入已有业务由本 Skill 完成；完全未实现的业务交给另一个 Skill，首次生成也遵守该边界。
- 以业务功能而非节点是否已有代码判断补充范围：新增视觉节点应接入已有状态与刷新逻辑，原本无业务的展示元素不扩展新功能。

- 源更新先对比新源、固定 `_UI` 与当前说明，差异经用户审核同意后才写入；后续按 MD 更新减法，再生成代码，仅修复或补全已有功能，不自动开发未实现功能。

- 已有界面更新默认维护固定 `_UI`，按新建主流程使用现成工具生成；取消默认另建时间戳整理稿、隔离候选再合并流程。
- 导出前读取当前两份说明与当前正式资产并保存可恢复的适配基线，生成后快速对比、恢复仍适用的布局和引用，再完成绑定及验证；本次差异已审核后不因重生成重复确认。
- 明确禁止修改框架、共享基类、PSD 导入/导出和 UI 生成/绑定工具源码，手写代码仅限业务/UI 逻辑；同步维护细则、模板与调用提示。

## 1.1.2 - 2026-09-16

- 明确文本适配按实际用途处理：固定文本只检查现有文案的对齐与显示，不额外做防爆框；动态文本才按业务预期长度、数值位数和换行需求判断并验证溢出风险。
- 文本框与文字按设计对齐，必要修正落实到 Prefab 配置；不强制所有文字居中、不为假设的无限长度过度适配、不以运行时字号或样式覆盖补救，测试后恢复预览文案。
- 固定 Prefab 说明与用户适配修订入口同步记录文本参数、处理依据、样本和实际验证结果。

## 1.1.1 - 2026-09-15

- 强化两步生成边界：PSD 转 Prefab、Prefab 生成代码与刷新绑定必须调用项目现成工具的菜单、按钮或同一方法入口；不允许拼接底层服务、自写替代生成器或手写生成结果，外部脚本也遵守此限制。
- 输出目录由当前工具配置及已确认参数解析，移除旧固定目录示例；候选隔离使用工具支持的方式，无法执行时报告限制。
- 明确外部后处理只调整工具已生成的 Prefab，并补齐实际入口、Importer、配置来源、隔离方式与输出记录，区分工具新写出、复用及获准迁移。

## 1.1.0 - 2026-09-15

- 整理生成 `_UI.psd` 时，关闭小眼睛的图层及隐藏组下的子层默认不进入输出，源文件完整保留；用户明确要求保留时才例外。
- 在原 PSD 修改说明中记录排除项与验证结果；仅改名、直接导出人工修正版及 Prefab 隐藏模板保持各自的处理边界。

## 1.0.1 - 2026-09-15

- 为无扩展名的 VERSION 显式固定 LF 换行，避免 Windows Git 自动转换；版本检查与更新行为不变。

## 1.0.0 - 2026-09-15

- 首个带版本号的发布版，承接已有 PSD → Prefab → 绑定与功能维护流程。
- 调用 Skill 开始任务时检查远端版本，展示新版本和变化；用户同意后由 Agent 更新本地，拒绝则继续当前版本。
- 更新固定到展示过的提交，保护本地修改与分叉历史；检查或更新失败时说明情况并沿用本地版本。
- 更新成功后重新读取 Skill 和本次需要的规则，交付时记录实际执行版本。

## 首次上传 - 2026-09-14

- 提交 `185c8f4`：上传流程 Skill、参考资料、两份溯源模板、只读 PSD 检查脚本和 README；当时尚未设置版本号。
