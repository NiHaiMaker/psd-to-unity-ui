---
name: psd-to-unity-ui
description: "把 PSD/PSB 整理为 Unity UGUI 界面，支持新建及已有 PSD→Prefab→绑定链条维护：750×1334、语义命名、Item、Prefab 减法、适配、现有工具绑定与溯源。用于 PSD 改名、生成 UI、人工修正整理稿后续作、更新 PSD 重生成并恢复正式适配、记录用户适配修正；支持指定阶段，PLink 项目接入现有 PSUIResolver 和 XUGUI。"
---

# PSD 到 Unity UI

把设计稿交付成可继续接入业务的 UI。一个 Agent 顺序推进有依赖的阶段；独立面板或只读复核才适合并行，同一 PSD、Prefab 和生成文件由一个执行者负责。

## 调用前检查版本

开始一项 PSD/UI 任务、尚未写入项目资产时，先读取并执行 [版本检查与更新](references/version-updates.md)。有新版时展示版本和变化，用户同意后由 Agent 更新；已明确授权本次自动更新则不重复询问。拒绝则按当前版本继续本次任务。检查失败或本地修改妨碍更新时保留本地文件并说明情况。更新后重新读取 Skill 及本次需要的资料，同一任务不循环检查；纯审核或维护本 Skill 时不自动更新。版本号以 [VERSION](VERSION) 为准，交付时说明实际使用版本。

## 默认设置与任务边界

- 本个人流程统一使用 **750×1334** 设计画布。当前任务明确要求其他尺寸时按新要求执行；遇到项目强制约束冲突先说明并按有效指令处理，不能静默修改整个项目的 CanvasScaler。
- 修改 PSD 时保留本次输入源文件，维护固定的 `<原文件名>_UI.psd`（PSB 保留扩展名）。已有对应整理稿时先保留可恢复的旧版本，再更新同一个固定工作稿；不默认另建时间戳 `_UI` 来导出。用户修正整理稿后直接从修正版续作。详见 [PSD 整理与样例](references/psd-preparation.md#源文件与输出命名)。只完成用户要求的阶段；只改名不调整画布、坐标、分组或图层取舍。
- 完整 UI 制作默认推进到 Prefab 输出、精简与布局、绑定生成及引用刷新全部完成；绑定属于完整流程，不再单独询问是否执行。具体功能在绑定完成后按当前需求实现；数据、点击行为、跳转、加载完成后的动作以需求或已确认接口为准，不从图片猜业务。用户只要求指定阶段时仍按指定范围交付。
- **两步生成必须使用项目现成工具：`_UI.psd / _UI.psb → Prefab` 使用现成 PSD 导入/生成工具；`Prefab → 代码与绑定` 使用现成 UI 代码生成/刷新工具。** 使用工具的菜单、按钮或其同一方法入口，沿用实际 Importer、当前配置及已确认的参数。不得自行编写或复制替代生成器、拼接底层服务另建生成流程、手写生成结果，或通过临时改输出配置、事后搬资源来代替工具正常输出。这是执行约束，不是优先推荐；新建、续作和重导入均适用。
- 自动化只负责通过项目允许的 Editor 通道调用上述现成入口，并检查结果；必要的最小调用代码不实现切图、从 PSD 组装 Prefab、代码模板或绑定赋值。把脚本放到 `Assets` 外也不能绕过这条边界；流程调用与后处理也不得向 `Assets` 新增辅助 `.cs`、自动绑定组件或永久菜单。工具不可用或失败时，核对实际状态并报告该阶段阻断，文件还原由用户处理，不自动回滚，不自行生成替代产物。整理与适配优先配置 Unity 内置和项目既有组件；相应外部后处理与按需求实现的功能代码遵守各自阶段边界，具体落点见 [Prefab 精简与布局](references/prefab-postprocessing.md#重导入与实际验证)。
- **本 Skill 不修改框架、共享运行基类、PSD 导入/导出工具或 UI 代码生成/绑定工具源码。手写 C# 仅维护本次明确的业务/UI 逻辑。** PSD、图片、Prefab 配置及现成工具正常生成的绑定仍按流程处理。遇到疑似工具缺陷，先核对当前固定 `_UI` 的内容、路径和实际导入版本，再报告具体证据与阻断；不能把“修复界面”扩展为修改工具。框架或工具开发须由用户另行提出独立范围的任务。
- 新建或改写的 Markdown、JSON、YAML、Python、C# 等文本使用 UTF-8；PSD、图片等二进制文件不转码。
- 每个界面固定维护 `01-PSD修改说明.md` 和 `02-Prefab减法与布局说明.md` 各一份，记录实际变化和理由。两份 MD 是完整迭代的共同依据：第一份解释源 PSD 到 `_UI` 的整理映射、视觉变化及人工修正，第二份解释 `_UI` 到 Prefab 的减法、布局、适配、绑定与已有功能接入。更新时先读两份，分别对比两个阶段的差异；经审核执行后同步维护两份，不能只读或只更新减法说明。源稿更新、人工修正和重新导出都在原文件内追加记录，不按轮次另建一套 MD；通过路径、记录编号及文件指纹串联，详见 [阶段溯源记录](references/traceability.md)。
- 已有界面源 PSD 更新时，先只读对比新源 PSD、当前固定 `_UI` 与当前说明，区分真实设计变化、历史整理差异和人工修正，形成具体差异清单供用户审核。**用户同意本次差异后，才更新 `_UI` 并进入生成流程**；笼统的完整流程授权不能跳过本次差异审核，已审核且未变化的范围不重复询问。之后使用现成工具生成 Prefab，结合当前两份说明的 PSD 整理映射和 Prefab 处理记录，与旧资产对比，恢复仍适用的减法、布局和适配并更新差异，再生成代码、修复旧逻辑、编译和刷新绑定。默认不复制新名字 `_UI` 或走候选合并分支。写入前执行 [正式写入前复核](references/existing-ui-maintenance.md#正式写入前复核)。
- PSD 更新引起的业务改动只做已有功能维护：受影响的旧逻辑修复，差异属于此前已实现功能的则补全其新节点、引用或数据接入；此前未实现的功能不自动补全，记录为未实现。判断单位是业务功能/状态，而不是单个视觉节点是否已有代码：新增节点若承接已有功能，必须补充相应绑定、状态控制和刷新逻辑，不能因节点是新的而跳过。已有功能以当前需求、旧代码和当前两份说明为依据，不能仅凭画面或曾计划实现认定。完全未实现的业务功能交由用户使用另一个 Skill 开发，本 Skill 只记录缺口及接入点。

## 先确定输入

从当前请求、目录和现有产物确认：项目根、Unity 工程根、原稿路径、可选整理后样例、输出 PSD 路径、模块名、面板名、目标设备范围和代码交付范围。能可靠推断的直接采用并说明；只对影响结果且无法推断的信息提问，不重复索要已经给出的内容。

路径中的 Markdown 转义不一定是真实目录名，必须核对磁盘。例如先确认 `AZ_RProject`，不要凭显示形式创建另一套目录。原稿、整理稿、Unity Prefab 是不同产物，不用“文件存在”推断阶段完成。

先遵守目标项目的 AGENTS.md、读取排除、版本控制和生成物边界。项目使用 `.cursor/` 作为 SSOT 时，按其入口完成启动和按需读取，本个人 Skill 只补充流程。只读检查本次目标状态并保留已有改动；SVN 调度、提交等操作遵循目标项目授权。

## 新建与维护入口

先检查是否存在对应的正式 Prefab、来源说明和绑定代码，再选择入口：

| 当前情况 | 执行路径 |
|---|---|
| 没有正式 Prefab | 按后面的新建阶段执行，在已授权范围内完成生成、精简、适配与绑定 |
| 源 PSD 有新一轮更新 | 先对比源稿、固定 `_UI` 与当前说明，提交差异审核；同意后更新 `_UI`、生成 Prefab、对照 MD 更新减法适配、生成代码并维护已有功能 |
| 用户修改了本轮 `_UI.psd`，或直接使用已有整理稿 | 核验当前修正版，以此输入执行主流程；已有界面先保存基线，生成后对照当前说明恢复适配 |
| 用户已手动修改正式 Prefab 的适配 | 以用户保存后的适配为最新方案，检查显示并维护原 `02-Prefab减法与布局说明.md`，不重导出或重跑绑定 |
| 已有 Prefab 的节点或绑定需要变化 | 核对受影响范围，提出正式 Prefab 改动清单及功能影响，按已有授权完成必要调整和现有工具绑定 |
| 仅修改手写功能代码，节点与绑定不变 | 按已明确需求直接修改并验证，不导出、不调整 Prefab、不重跑绑定；详见 [纯功能修改](references/existing-ui-maintenance.md#仅修改手写功能代码) |

命中任一维护入口时读取 [已有链条维护](references/existing-ui-maintenance.md)。源稿本身一般不表达运行时适配；当前正式 Prefab 和已记录的用户适配修正是维护时的保留依据。每次 PSD 重生成并恢复适配后都运行现有绑定流程；用户已经自行完成的纯适配修改只检查和记录。用户委托 Agent 尚未执行的适配调整，仍按正式改动清单及已有确认执行，不能误当成只需记录。

## 新建阶段与共同操作

PSD 阶段确定视觉素材、语义命名和候选 Item；导入后的 Prefab 再落实运行节点取舍、组件、锚点和布局。完整链路为：**保留源文件、维护固定整理稿并记录修改 → 检查整理稿及人工修正 → 调用现有方法输出视觉 Prefab → 精简绑定与唯一 Item → 锚点与布局并记录原因 → 调用现有绑定生成、必要手写引用修复、编译、刷新 → 补齐已有功能的差异接入 → 验证**。已有产物从实际未完成阶段继续，后处理不隐含回写 PSD。

### 1. 盘点与整理 PSD

读取 [PSD 整理与样例](references/psd-preparation.md)。可用 [图层检查脚本](scripts/inspect_psd.py) 输出尺寸、图层层级、可见性、文本和源文件摘要；用视觉与结构共同识别图层用途。

按 [阶段溯源记录](references/traceability.md) 建立或维护固定的 `01-PSD修改说明.md`，在本次记录中填写源文件指纹和操作意图，再在已保留旧版本的固定工作稿中执行：统一画布、语义命名、功能分组、绑定标记、保留/排除图层。说明文件的建立不是默认审批门；已有足够授权时继续执行。保存后重新打开整理稿，核对结构、尺寸、文字与图像，补齐实际修改、原因、输出指纹和源文件未变的验证；不要仅检查保存动作返回成功。

整理生成 `_UI.psd` 时，默认排除自身或祖先组关闭小眼睛的图层，隐藏组下的子层也不进入输出；保留源稿，在原第一份说明中记录排除项。用户明确指定保留时才例外处理。仅改名及直接导出人工修正版仍按原任务边界执行，详见 [隐藏图层排除](references/psd-preparation.md#隐藏图层排除)。

本次包含分类或分组时，主动检查重复结构是否适合作为通用 Item，例如图标与数量、图标与冷却、列表单元格。PLink 项目按 [通用 Item 判定](references/plink-integration.md#通用-item-判定) 确定模板、子节点作用域和外观参考，不能只整理普通分组而漏掉 Item 边界；只改名任务仍遵守用户指定范围。

PSD 编辑应使用已核实可保留图层、文本、蒙版和智能对象的工具。附带脚本只做只读盘点，不负责重命名、缩放或导出 Unity 资源。具体工具、检查条件和无法执行时的交付边界见上述参考。

### 2. 生成视觉 Prefab

每次导出（包括只导出已有稿和重导入验证）先执行 [导出前整理稿检查](references/psd-preparation.md#导出前整理稿检查)。人工修正版是本轮续作依据，不重新从源生成来覆盖它；符合导出要求就继续，不增加固定确认点。发现无法导出的冲突或无法判断的意图时，列出具体问题再由用户决定。通用纠错反馈生成与检查规则，单页处理留在该界面说明中。

PLink 项目先读 [PLink 接入与生成边界](references/plink-integration.md)，路由到目标项目当前的 PSD/UI 技能与工具。非 PLink 项目使用其实际导入器与代码生成方式，不能套用 PLink API、目录或命名后缀。

先核对项目现成 PSD 工具的实际 Importer、菜单/按钮入口、调用参数与输出范围，再从同一入口生成；不能以“调用了现有底层代码”代替执行现成工具。生成前按当前配置解析本次产物的输出位置，完整流程同时规划绑定代码目录，外部自动化与 Unity 资源分别存放。盘点可复用资源及已有 GUID，按实际内容和导入设置判断复用，不能因同名或文件存在就跳过更新；缺失或变化的图片通过同一工具按当前配置导出，共享资源的更新与引用影响纳入本次范围。PLink 细节见 [资源与导入](references/plink-integration.md#资源与导入)。核对导入器当前支持的组件和命名指令，缺失能力先用内置或项目既有组件补齐；仍需新脚本时说明剩余能力及阶段边界，不因后处理自动新增辅助代码，不凭图层名宣称自动生成了按钮、列表或业务逻辑。

生成后核对层级、图片/字体引用、文本内容、透明度、遮挡顺序、蒙版、九宫格和组件类型。导入 Prefab 与最终适配产物可能有不同的覆盖边界，先确认再修改。

已有正式 Prefab 时，先按维护入口保存可恢复基线，核对工具对正式路径、资源及 `.meta` / GUID 的覆盖行为，再按新建流程生成。之后通过当前说明和旧资产快速比较，恢复仍适用的组件、布局、Item 与引用；不从零重新猜测适配，也不机械重放过时参数。工具无法安全完成该流程时报告准确阻断，不修改工具源码。

将导出入口、影响结果的配置、所用 PSD 的路径及指纹写入 `02-Prefab减法与布局说明.md`，链接第一份说明；记录刚导出、尚未后处理的 Prefab 状态，作为后续比较基线。

### 3. 精简绑定与唯一 Item

读取 [Prefab 精简与布局](references/prefab-postprocessing.md)。在已确认的正式 Prefab 改动范围内，基于实际运行职责精简绑定：静态装饰和纯布局容器保留视觉，移除无用绑定标记；动态内容、交互和独立状态保留必要引用。已有业务引用须先检查调用方，不能仅凭画面猜测不用。维护任务先恢复已记录且仍适用的设置，再处理新增或变化节点的适配。

将重复结构收敛为一个模板与明确的实例方案，确认图标、文本及其他外观差异均能保留后，再从最终运行 Prefab 移除不参与运行的参考节点。仅去掉参考组名字或隐藏它们，不等于完成复用。记录后处理操作及稳定定位方式，让重导入后可以重放。

在第二份说明中逐项写明节点原状、实际修改、需求依据和影响；去绑定标记、删除节点、模板复用分别记录，不能只写“精简”而不解释原因。

### 4. 锚点与布局

同时检查设计稿文字的 [描边与阴影](references/prefab-postprocessing.md#文字描边与阴影)：缺失时在 Prefab 编辑阶段补齐对应内置或项目既有组件；TMP 使用其 Shader 支持的材质效果。保护共享材质，不重复叠加已有或烘焙效果，不在运行时补组件。固定文本也检查这些视觉效果，记录到原第二份说明并纳入适配恢复。

以本次确认的目标尺寸为设计坐标（完整流程默认 750×1334），按语义设置锚点、布局与安全区：背景保持比例并覆盖画面；logo 保持比例；进度条的槽与填充作为同一功能组；文字检查边距和可读性。具体对齐位置以设计稿为准，不能把样例的所有中心锚点当成通用规则。

同级序列按真实排列使用 HorizontalLayoutGroup 或 VerticalLayoutGroup，以 LayoutElement 表达单元尺寸；只在容器需随内容伸缩时使用 ContentSizeFitter。同一 RectTransform 的同一轴只保留一个尺寸控制者，不能让手写定位与布局组件同时驱动位置。组件配置、模板是否占位及验证方法见上述参考。

文本适配按 [文本居中与溢出](references/prefab-postprocessing.md#文本居中与溢出) 区分用途：固定文本只核对实际文案的对齐与显示，不额外做防爆框处理；动态文本才按业务预期的长度、数字位数和换行需求判断并处理溢出风险。按设计同时核对文本框与文字的水平/垂直对齐，调整 Prefab 配置，不把所有文字强制居中，也不用运行时改字号或样式补救；在原第二份说明中记录实际变化与验证。

检查本次基准尺寸和任务要求的其他宽高比。未指定设备时，可先采用基准尺寸与一个长屏比例做最小预览，并明确尚未覆盖的设备；平板适配仅在任务包含时纳入。背景延伸、裁切不得裁掉关键交互和文字。

对于会被重新导入覆盖的适配，在当前说明中保存可恢复的具体参数与稳定节点定位，并保留旧资产；不强制新增配置或脚本。已有变体、配置或外部后处理可复用时，先核对实际覆盖行为并更新过时参数。后处理沿用项目指定的 Editor 自动化通道，本轮重新生成后核对适配恢复结果，不用新增工程内辅助脚本或手改生成文件解决覆盖问题。

完成后补齐第二份说明的布局参数、锚点理由、实际预览和重排结果，记录“布局完成”时的 Prefab 指纹及外部后处理入口。

### 5. 调用现有绑定流程

完整制作任务在精简与布局完成后自动进入本阶段；每次 PSD 重生成并恢复正式适配后都执行本阶段。用户仅修改适配时只检查显示并更新原说明，不重新运行绑定。仅修改手写功能且现有节点与绑定仍满足需求时跳过本阶段。其他只做 PSD 或 Prefab 指定阶段的任务按该范围结束并说明绑定尚未执行。必须调用项目现成 UI 工具的代码生成与刷新入口；面板、Item、Mono、Auto 及工具提供的初始骨架均由该工具生成，不自行照抄模板生成文件或写绑定器。

先核对最终节点、组件及生成作用域，再调用项目权威生成器。PLink/XUGUI 的顺序为：**工具生成代码 → 修复必要的手写引用 → Unity 编译通过且生成类型可加载 → 刷新 Prefab 引用 → 检查回写结果**。刷新本身可能重写代码和 Prefab。

节点更名、删除、组件类型或 Item 边界变化导致旧手写代码不再适用时，生成后按新字段和类型完成已确认范围内的必要引用迁移及类型适配，再等待编译通过；不能把这些修复推迟到绑定刷新之后。无此类变化则跳过修复。生成文件仍由工具维护，既有清单内修复无需二次确认；其余已有功能的差异接入在绑定通过后完成。

绑定刷新后更新第二份说明，单独记录“绑定完成”时的 Prefab 指纹、生成文件位置和引用检查结果，保留前面的布局状态；后续功能若再次修改 Prefab，追加对应状态。

生成文件与手写文件按工具的实际覆盖范围区分。需要访问的节点使用生成字段；引用缺失就修正命名/组件并重新生成刷新，不能添加按名称搜索或遍历子组件的兜底来掩盖失败。

### 6. 维护已有功能与新增视觉接入

纯功能修改可从本阶段进入：只读核实现有引用有效后按已明确需求修改手写代码，不要求先生成、刷新或确认空的 Prefab 清单。发现确需调整节点、绑定或正式资源时，才进入相应改动清单及授权范围。

必要的手写引用修复在上一阶段完成。本 Skill 维护已有业务功能，包括将新增视觉节点接入现有状态、显隐和刷新逻辑；例如已有选中状态而新设计增加选中框，就在此完成选中框绑定和状态切换。此前完全未实现的业务功能交由用户使用另一个 Skill 开发，本 Skill 记录缺口和接入点；首次生成界面也遵守此边界。用户已确认 Prefab 清单及对应功能影响时，不另设代码确认步骤；出现超出清单的改动再说明新增范围。交付已绑定的 Prefab 与现有生成器所需骨架，不虚构业务规则、数据来源或导航流程。

手写代码只实现本次明确的界面展示、事件和生命周期。业务状态变更通过业务实体/管理器接口；注册、解绑、回池清理遵守框架。用户要求预留下一步时保留约定的接口，不自行决定导航或业务动作。

### 7. 验证与交付

按实际完成阶段列出证据：

| 阶段 | 通过条件 |
|---|---|
| PSD 整理 | 画布符合本次确认尺寸（完整流程默认 750×1334；只改名保留原尺寸）；操作映射与保存后结构一致；整理输出按小眼睛状态排除隐藏层并记录明确例外；原稿和人工修正保留；导出前检查对应实际使用版本；文字、图层效果和关键视觉可用 |
| Prefab | 层级/组件符合需求；图片字体引用有效；根 Transform 和 Canvas 正常 |
| 精简与复用 | 静态视觉保留；已用绑定未丢失；Item 模板唯一且实例方案落实；无重复参考显示或模板占位 |
| 适配 | 基准及已选宽高比有预览证据；固定文本按实际文案对齐与显示，不要求动态防爆框；动态文本在业务预期范围内重排后无意外裁切、重叠或爆框，确需采用的超长策略符合可读性要求；关键元素不遮挡、不越界；动态列表变化后布局正确；再次生成可保留或重放调整 |
| 代码 | 涉及绑定生成时权威生成器完成；相关程序集编译；序列化引用有效；手写代码不在覆盖目标中；纯功能修改验证对应行为，沿用有效绑定 |
| 运行 | 在目标项目实际入口验证显示、隐藏、重开和明确要求的交互；涉及池化时验证清理 |
| 溯源 | 本次执行阶段的说明已完成；源文件未变、输入输出指纹可对应；操作理由、验证状态和布局/绑定版本清楚 |

静态盘点、源代码检查、Editor 生成、编译和 PlayMode 验证分别报告。工具失败或中断时停止依赖该步骤的写入，报告已完成项、已写入文件与当前状态；文件还原由用户处理，不自动回滚或以重试继续覆盖。正常生成后的适配恢复仍属于本 Skill 的职责，不等于异常回滚。不能伪造后续产物或宣称整条链完成。

审阅目标版本状态与差异，输出产物路径、主要变更、已做/未做验证及业务接入点。任务记录放目标项目允许的位置；原始 PSD、美术文件、运行结果和机器配置不放入本 Skill 目录。

## 调用与维护

调用示例：`使用 $psd-to-unity-ui，把指定 PSD 整理为 750×1334，调用项目已有工具输出 Prefab，完成减法、布局、绑定及已有功能的差异接入。` 已有链条可说：`用新的源 PSD 更新现有界面，维护固定 _UI，按新建流程生成，再通过当前说明与旧资产恢复适配并验证。` 整理稿续作可说：`我改了 _UI.psd，以这份修正版继续导出，更新原修改说明。` 适配修订可说：`我改了正式 Prefab 的适配，检查显示并更新原修改说明。` 只处理部分阶段时明确指定，例如：`本次只整理 PSD`。

本目录可独立用 Git 维护，内部资料使用相对链接。`agents/openai.yaml` 提供 Codex 的显示名称与调用提示，不会创建独立 Agent；技能保持默认自动匹配。不要把整个 Codex 配置目录、第三方依赖或项目资源纳入本技能的版本管理。
